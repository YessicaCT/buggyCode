function trickOrTreat() {
    let treats = ["Candy", "Chocolates", "Lollipops"];
    for (let i = 0; i <= treats.length; i++) {  
        console.log("You received: " + treats[i]);  
    }
    console.log("Total treats: " + treats.count);  
}

function hauntedHouse() {
    let ghosts = 10;
    let hauntedMessage = "You see " + ghosts + " ghost!";  
    if (ghosts = 0) {  
        console.log("No ghosts here!");
    } else {
        console.log(hauntedMessage);
    }
    return ghost;  
}

trickOrTreat();
hauntedHouse(); 
